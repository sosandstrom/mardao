package ${daoPackage};

import android.database.sqlite.SQLiteDatabase;

/**
 * Implementation of Business Methods related to entity ${entity.simpleName}.
 * This (empty) class is generated by mardao, but edited by developers.
 * It is not overwritten by the generator once it exists.
 *
 * Generated on ${currentDate}.
 * @author mardao DAO generator (${helper.class.name})
 */
public class ${entity.simpleName}DaoBean 
	extends Generated${entity.simpleName}DaoImpl
		implements ${entity.simpleName}Dao 
{
    /** Constructor */
    public ${entity.simpleName}DaoBean(SQLiteDatabase database) {
        super(database);
    }

    /**
     * Implement this tables upgrade method here
     * @param sqld
     * @param fromVersion
     * @param toVersion 
     */
    public static void onUpgrade(SQLiteDatabase sqld, int fromVersion, int toVersion) {
        for (int v = fromVersion; v < toVersion; v++) {
            switch (v) {
                case 0:
                    // do nothing, as first version should be 1
                    break;
                default:
                    throw new UnsupportedOperationException("Not supported yet.");
            }
        }
    }
    
	// TODO: implement your Business Methods here

}
